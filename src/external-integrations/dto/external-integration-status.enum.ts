/**
 * Enum que define los posibles estados de una integración externa
 */
export enum ExternalIntegrationStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  ERROR = 'error',
  DELETED = 'deleted'
} 